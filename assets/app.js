document.addEventListener('DOMContentLoaded',()=>{const b=document.querySelector('.menu-toggle'),n=document.querySelector('.main-nav');if(b&&n)b.addEventListener('click',()=>n.classList.toggle('open'));document.querySelectorAll('[data-search]').forEach(input=>{const target=document.querySelector(input.dataset.search);if(!target)return;const cards=[...target.querySelectorAll('[data-title]')],empty=target.parentElement.querySelector('.empty');input.addEventListener('input',()=>{const q=input.value.trim().toLowerCase();let shown=0;cards.forEach(c=>{const ok=c.dataset.title.includes(q);c.classList.toggle('hidden',!ok);if(ok)shown++});if(empty)empty.style.display=shown?'none':'block'})});document.querySelectorAll('img').forEach(img=>{img.addEventListener('error',()=>{img.style.display='none';img.parentElement.classList.add('image-failed')},{once:true})})});